import 'package:flutter/material.dart';

class AppColors {
  static const white = Colors.white; // Represents the white in the flag
  static const black = Colors.black; // Can stay for text or darker accents
  static const primary = Color(0xffED1C24); // Red from the Lebanese flag
  static const secondary = Color(0xff00A651); // Green from the Cedar tree in the flag
  static const tertiary = Colors.redAccent; // Optional: You can keep or replace this
  static const lightGray = Color(0xffCCCCCC); // Light gray for subtler accents
  static const lighterGray = Color(0xffF5F5F5); // Lighter gray, close to white for backgrounds
  static const transparent = Colors.transparent; // Keep transparent
  static const lighterBlack = Colors.black45; // Can be used for shadows or softer black
}
